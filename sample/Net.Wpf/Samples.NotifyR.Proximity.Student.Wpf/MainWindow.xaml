<Window x:Class="Samples.SignalR.Proximity.Student.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Samples.SignalR.Proximity.Student.Wpf"
        mc:Ignorable="d"
      Title=".Net 4.7.2 (Notifier)" Height="550" Width="800" Background="#30FFFFFF">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content=".Net 4.7.2 (Notifier)" HorizontalAlignment="Center" FontSize="40" Foreground="LightBlue"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Content="Title:" HorizontalAlignment="Right" Margin="5" Foreground="Orange"/>
            <Label Content="Message:" HorizontalAlignment="Right" Grid.Row="1"  Margin="5" Foreground="Orange"/>

            <TextBox Text="{Binding MessageTitle,UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Width="220" Margin="5"/>
            <TextBox Text="{Binding Message,UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Grid.Row="1" Height="240" AcceptsReturn="True" Width="220" Margin="5"/>
            <Button Content="Notify Information" Click="SendInformation"  Grid.Column="1"  Grid.Row="2" Width="220" Margin="5"/>
            <Button Content="Notify Success" Click="SendSuccess"  Grid.Column="1"  Grid.Row="3" Width="220" Margin="5"/>
            <Button Content="Notify Warning" Click="SenWarning"  Grid.Column="1"  Grid.Row="4" Width="220" Margin="5"/>
            <Button Content="Notify Error" Click="SenError"  Grid.Column="1"  Grid.Row="5" Width="220" Margin="5"/>
        </Grid>
    </Grid>
</Window>
