<UserControl x:Class="Samples.Framework.WPF.Concepts.SampleStep.SampleSteManagerMenuViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Samples.Framework.WPF.Concepts.SampleStep"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
    <Grid >
        <ListView 
            ItemsSource="{Binding StepsTems}" 
            SelectedItem="{Binding CurrentStep}"
            Width="240" Background="#30FFFFFF" Foreground="LightBlue" FontWeight="DemiBold">
            <ListView.ItemTemplate>
                <DataTemplate >
                    <Border >
                        <TextBlock Text="{Binding MenuTitle}"/>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
